# Optional runtime overrides. Synod is local-only by default.
SYNOD_DATABASE=synod.db
SYNOD_BIND=127.0.0.1:3030

# Provider environment-variable names are chosen in the Web UI. For example:
# DEEPSEEK_API_KEY=
# MINIMAX_API_KEY=
